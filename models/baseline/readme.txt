To install scilearn-kit, execute the following commands:

conda create -n sklearn-env -c conda-forge scikit-learn
conda activate sklearn-env

The program svm.py should run as expected after performing the above steps.